import pygame

cell_size = 40 # Defines the size of a cell
cell_number = 20 # Defines the number of cells that are in the screen
screen = pygame.display.set_mode((cell_number * cell_size, cell_number * cell_size)) # Initializate the screen to be viewed